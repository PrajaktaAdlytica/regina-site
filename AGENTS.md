# Prototype Instructions

Latest approval: publish the complete reviewed website to the existing live Regina domain after checks. The book is the homepage hero; preserve approved headlines. Its left pages use ivory for education, deep plum with inverse text for innovation, and blush for cooperation, including the turning sheet. Remove prototype/concept labels from presentation while retaining central image provenance and required licences. This supersedes the review-only publication restriction below.

September 8 client revision: work on review/hubert-home and Vercel preview deployments only. Do not push main or promote to production until the user approves the completed revisions. First review increment: capitalise Home’s opening Od and replace the existing Accelerate illustration banner with the IPPP reference’s original-logo/copy/CTA and red Poland-map panel, using a smaller map. Keep the confirmed acceleratepoland.org destination. Remaining transcript changes will be handled one at a time after review. This supersedes older conflicting banner requirements below.

3D availability correction — 8 September 2026: the approved Home sculpture must render on compact laptop/browser viewports from 700×520 upward. A system reduced-motion preference no longer swaps it for an unrelated programme illustration; it keeps the same real 3D scene stationary and disables the motion toggle. Retain the lightweight illustration fallback only for smaller/mobile viewports, Save-Data, or genuine WebGL/context failure. This supersedes the older 900×700 and reduced-motion fallback threshold.

User authorised committing the complete current site to https://github.com/PrajaktaAdlytica/regina-site.git and publishing a public Vercel review for Hubert. Vercel project: regina-site in prajaktagaikwad574-2789s-projects. Preserve existing Sites origin remote/config; use separate github remote. Vercel serves dist/client with explicit static configuration and security/noindex headers, not the Cloudflare worker. Contact remains an email-draft flow. This authorises the requested Vercel publication, not a new deployment of the previous Sites URL or custom-domain DNS changes.

Accelerate destination confirmed by user: use https://www.acceleratepoland.org for ALL Accelerate links and acceleratepoland.org for displayed domain text, including Home CTAs/list, Projects card and brand-kit specimen. This supersedes earlier instructions retaining .org.pl or awaiting a Vercel substitution. The .org address responds successfully but currently serves a coming-soon page; no DNS or Accelerate-site deployment was requested. Preserve design/branding/motion. Public Regina update approval remains pending.

Project branding compliance: use actual client/project marks on the three Projects cards, replacing generic category icons. Accelerate uses the original received PNG export from its previous project (not a new tracing); ASEAN uses its original Wix header logo including ARF; SAFTA uses its original white SVG on a dark slate backplate. Preserve colours, whole-image contain fitting, existing project titles, card wipes and GSAP wrappers. Home's early Accelerate banner also uses the original logo, retaining its illustration and CTA. Sources recorded in public/site/project-brands/SOURCES.md. Supplied Accelerate domain still NXDOMAIN as of September 3; do not silently replace with a preview URL. Asked user whether to use working Vercel site temporarily. Public update approval remains pending.

## Regina decisions — 3 September 2026

Homepage brief-compliance correction: user requests restoring Hubert's Home headline and CTA/content placement before addressing other QA findings. Use the complete exact red hero title (foundation name styled as the small first line within H1), unchanged supplied subtitle, and both org.intro/org.purpose paragraphs. Opening “Poznaj nasze programy” links directly to the supplied Accelerate Poland URL with project context/new-tab notice. Move the single existing illustrated Accelerate banner immediately after the introductory paragraphs, ahead of programme cards, and use the supplied CTA wording. Preserve plum palette, logo, artwork, 3D hero, hover and scroll lifecycle; other pages unchanged. Supersedes prior preservation of the shortened H1 and /dzialania/ opening CTA. Retain the supplied project URL despite the separately reported DNS issue. Local revision; public publishing approval still pending.

Home intro palette update: full-width plum surface behind the existing two-column foundation introduction, ivory text, rose eyebrow/emphasis/marker/muted copy, inverse programme CTA with blush wipe and light focus outline. Keep original wording, spacing, inner .home-intro scroll anchor, sculpture and other sections. Navbar Contact envelope stays visible at rest; other category icons retain hover-only reveal. Local only.

Navbar density correction supersedes previous preserved-height instructions: shared inner height 92px desktop / 80px mobile instead of 176/144px; remove Misja's old height exceptions. Original logo asset fitted at 148×60 desktop / 132×54 mobile, tighter 16px nav gaps, blush outlined Contact destination and mobile Menu button. Keep sticky position, dynamic anchor/sculpture offsets, original logo/links, underlines and icon reveals. No new navbar links or layout changes below header. Local only.

Shared navigation stays sticky at top on all website routes (including mobile and legal pages), retaining original logo, dimensions, Misja-specific density and hover/menu behaviour. Opaque canvas background with subtle shadow, above page animations and below the modal/skip link. Measure actual header height with ResizeObserver for native scroll padding and Lenis anchor offsets. Homepage pinned sculpture top/progress/jumps account for that height; short desktop viewports use its existing compact fallback. Local only.

Contact redesign: compact editorial opening, plum direct-contact card beside blush enquiry form, separate blush Warsaw city-map section, split FAQ. Preserve all confirmed contact/registration details and honest email-draft form with existing validation/topic-prefill. Replace Poland outline with opt-in OpenStreetMap city-wide iframe (no office marker or invented address); existing credited Warsaw photo before activation, external fallback, toggle-off and privacy disclosure. Contact-specific lazy GSAP/Lenis replays every section/individual contact, form and FAQ row with alternating/reversed horizontal entrances in both scroll directions. Focus immediately restores controls, native mobile/reduced-motion and cleanup retained. Wine directional contact-row wipes and FAQ/field/map-photo hover/focus. Local only; public update still requires approval.

Partnerstwo hover: partner categories have alternating left/right blush wipes, wine rule draw and icon lift/tilt; offer cards have bottom-up blush wipes and restrained title/step movement. Original collage and introduction artwork lift subtly; network panel responds to hover or contact-link focus. Keep layouts/copy/assets, informational card semantics and GSAP independent translate intact. Fine-pointer hover and reduced-motion instant-colour/no-movement safeguards. Local update only.

Partnerstwo scroll motion: preserve all existing layouts, collage/illustrations, copy and contact links. Lazy-load shared GSAP/Lenis with per-section and individual partner/offer-row stationary triggers. Alternate left/right, reverse on upward re-entry, replay every pass; 96px/1.05s desktop and 28px/.65s mobile. Footer uses the same horizontal profile. Preserve native touch, reduced-motion handling, immediate focus visibility and unmount cleanup. No public deployment implied.

Projects reveal emphasis follow-up: animate each complete card (including surface) inside a stationary project-reveal-row, alternating left/right by row and reversing on upward re-entry. Projects section travel is now 96px/1.05s, mobile 28px/.65s. Footer columns also use the Projects horizontal/reverse profile through an optional shared-controller footer specification. Other page defaults, card hover and layouts remain unchanged.

Projects refinement: redesign ONLY the breadcrumb/title/introduction as an editorial headline plus compact blush introduction/count panel; count derives from the three actual projects. Preserve collage, card layouts, context illustration, approved copy and external project destinations/new-tab notices. Add card-wide directional colour wipes (wine left-to-right, stone right-to-left, plum bottom-up), subtle title/icon response and keyboard focus-within equivalent; keep readable text during wipes and reduced-motion instant states. Projects now lazy-loads the shared GSAP/Lenis lifecycle with alternating horizontal reveals for collage, each project card, context and CTA, reversing on upward re-entry. Other page defaults unchanged; public update approval pending.

Działania research hover: preserve pink section and non-interactive subject rows. Each hovered knowledge row gets a left-to-right rose wipe, wine heading, 8px heading inset and small books-icon tilt. Section hover/CTA keyboard focus draws an underline beneath the italic statement. Scope to Activities, keep GSAP independent translate, fine-pointer hover only and reduced-motion instant colour/no movement. Do not invent publication links.

Działania opening redesign: compact editorial split with the original headline/copy and a blush six-programme index, numbered native anchor links, established category icons and directional hover. Keep all six photographic programme rows and confirmed content. Activities lazy-loads the shared GSAP/Lenis lifecycle; each index row, programme row, research row and CTA reveals horizontally in both scroll directions, reversing its left/right origin on upward return. Home/Misja defaults unchanged. Native touch, reduced-motion, focus visibility and full cleanup remain. Public-update approval still pending.

Mission statement hover: decorative left-to-right deep-plum sweep over the wine surface, brighter divider draw and subtle target-icon tilt/scale. Preserve mission wording, layout and existing GSAP child entrances. Fine-pointer hover only; instant colour feedback without icon movement for reduced motion. Do not add fake click affordances or a redundant keyboard stop to this reading section.

Warsaw bridge scroll treatment: dedicated expanding-frame reveal (inset rounded window to full width) and gentle zoom-out, restarted by GSAP's existing visible/ready state on BOTH downward and upward re-entry. Shared slide targets the caption only, not the image frame. Original Warsaw image, caption and Arne Müseler credit remain; lighter mobile motion and reduced-motion static fallback. Anchor /misja/#warszawa.

Misja directional-motion emphasis: make entrances clearly visible from left/right/top/bottom in BOTH scroll directions. Mission groups now use 72px travel over 0.9s (mobile 28px/0.6s), power2.out; existing onEnter/onEnterBack replay and vertical direction reversal preserved. Do not hide settled content or animate section background blocks. Homepage defaults unchanged.

Photo label cleanup: user requested removing “Fotografia tematyczna” tags everywhere. Remove the hero overlay and repeated thematic-photo/source notes under photo pillars, activities, mission atlas and programme explorer. Keep meaningful alt text, central /informacje/#fotografie credits/licences/disclosures, and required Warsaw photo attribution. Do not delete source assets or imply thematic photos document the foundation. This supersedes earlier requirements for inline thematic-photo tags.

Misja scroll motion: use the same GSAP ScrollTrigger/Lenis lifecycle as Home via installPageScrollMotion, with a separate missionRevealGroups configuration and lazy-loaded engine. Replay directional entrances on down/up re-entry for hero, statement, atlas, international network, community, city image, CTA and footer. Animate atlas/network outer wrappers only, preserving their internal selection transitions. Keep native touch scrolling, reduced-motion handling, focus visibility, font/resize refreshes and full cleanup. No layout redesign or public deployment implied.

Misja hero photo scale: user wants a slightly larger picture to occupy the centre whitespace. Desktop/tablet grid is now 1fr/1.08fr, desktop column gap 48px, and desktop photo left padding removed; photo height and type sizes unchanged. This widens the picture toward the centre without lengthening the hero. Mobile keeps its existing inset/stacked format.

Misja hero density: retain the approved staggered-type/curved-photo structure but tighten whitespace. Misja-only header height 136px desktop/104px mobile, hero top margin 8px, desktop row gap 28px and bottom margin 48px (24px/40px mobile). Preserve type sizes, photo dimensions, logo and interactions; other page spacing unchanged.

Misja hero refinement: replace the plain flush split with larger staggered heading, offset curved-corner mentoring photo with blush backing, circular six-goal anchor and separate introduction/CTA row. Keep original message, photograph, attribution, two destinations and official logo. Remove both hero top rule and shared header bottom rule on Misja only; keep nav active/hover underlines and other pages unchanged. Responsive stacked layout and reduced-motion styling required.

Misja community section: keep the exact community.webp illustration and all four people/wheelchair, but change its presentation from a rectangular split image/text block to an open editorial composition. Large heading above, whole uncropped illustration blended onto the page with a soft blush halo, supporting copy/themes and existing activity CTA alongside. Do not regenerate, replace or crop the picture. The international network above is unchanged.

International section revision: user rejected the static white illustration panel but approved the pink background. Supersede the editorial artwork/four-link design below with a pink-surface interactive cooperation network. Four native region buttons select on mouse hover, keyboard focus or tap; animate the selected connection from Poland and update the region-specific enquiry CTA. No autoplay, invented office/project locations or geographic accuracy claim: label this a conceptual cooperation diagram. Preserve the approved copy, other sections, and original illustration assets.

Misja international section: replace the Poland-only geography outline with InternationalCooperation. Emphasize people/globe artwork and the four client-confirmed regions (Europe, Africa, Asia, Australia), retaining Poland only as the starting-point label. Region links open a clearly labelled cooperation enquiry with prefilled topic, not invented regional project pages/offices. Reuse the supplied international programme wording and original illustration. Preserve the Poland map assets for appropriate local contexts; do not delete them globally.

Misja six-goal section: replace goal-grid with MissionAtlas, a two-column selector grid paired with an illustrated chapter panel. Preserve all six original goal titles/descriptions. Click/tap or keyboard selects; previous/next wraps; GSAP animates only the selected artwork/copy with reduced-motion support and cleanup. Do not auto-rotate or select on hover. Use existing brand art/credited thematic photos and actual related activity/partnership links; never imply photos depict foundation projects. Other Misja sections and Home remain unchanged.

Homepage scroll motion: Home-only lazy-loaded GSAP ScrollTrigger + Lenis in home-scroll-engine.js/home-scroll-motion.js. Trigger stationary parent groups and animate children with opacity + independent translate so hover transforms are preserved. Alternate side/top/bottom entrances with restrained stagger; replay after leaving the viewport in either direction, reversing vertical offsets on upward re-entry. Lenis smooths fine-pointer desktop wheel input, uses the GSAP ticker, and coordinates hero chapter jumps through scrollHomeTo; touch/mobile scrolling stays native. Keep settled content visible and the existing 3D hero/layouts. Reduced-motion changes cancel reveals and destroy Lenis immediately; focused groups are shown instantly. No public deployment implied.

Latest Home Działania decision: replace the open six-icon grid with ProgrammeExplorer, adapting the user's supplied TeamShowcase interaction (not its sample people, social links or framework migration instructions). Desktop: staggered 3-column artwork/photo mosaic linked on hover/focus to six numbered programme links; a stable description panel below the mosaic. Mobile/tablet <=900px: native buttons expand one descriptive panel at a time with a real programme link. Retain Cormorant/DM Sans, wine/ivory, original illustrations and credited local stock images. No new dependencies or page routes. Older instructions requiring the six-icon grid are superseded only for this section; keep the three photographic cards and their hover treatment unchanged.

Home photographic cards 01–03: coordinated hover across the entire article, using a bottom-up blush background wipe (420ms in / 240ms out), subtle photo zoom/saturation, burgundy heading, slight icon lift and right-moving CTA arrow. Preserve existing layout, readable copy and real links. Keyboard focus-visible within the card gets the same treatment; hover motion is limited to fine pointers. Reduced motion keeps instant colour feedback without zoom/lift/arrow movement.

Navbar refinement: desktop link underlines draw left-to-right on hover and keyboard focus in 300ms. Keep existing icon reveal and persistent current-page underline. Reduced-motion preferences disable the transition. Apply through shared site navigation, not unrelated footer or brand-kit specimens.

Current scope/status overrides historical notes below: the multi-page site is implemented; the existing Sites review is public with noindex. The latest screenshot correction is built and locally verified, but publishing that revision awaits the user's reply to the public-update approval question. Do not interpret older owner-only or kit-only statements as current. Read the newest sections of BUILD-REVIEW.md and design-qa.md first.

Screenshot fidelity correction: the seven screenshots provided at 10:39–10:40 are implementation requirements, not loose inspiration. Home uses numbered photographic columns and a separate open six-icon overview; Mission uses a split photographic introduction; Activities uses alternating flush monochrome photo/text rows; Partnership uses a torn-paper portrait collage; Projects uses a connected three-theme collage. Retain original supplied logo, established tokens/hover states, human illustrations and the approved 3D scroll experience. Use real credited thematic photographs, explicitly not foundation staff/project documentation. Never invent publication covers or report titles: the research editorial uses confirmed subject areas until actual publications are supplied. Existing Sites URL is public since the user requested public access; retain noindex review status.

Website build now authorised: src/site is the six-page Polish website plus privacy/legal pages. The old motion study is preserved at /motion-study.html. Latest source content and launch gates are in BUILD-REVIEW.md. Do not interpret earlier kit-only scope as prohibiting the current full-site build. Contact is currently an explicit email-draft flow, not a configured server submission. Preserve the original worker/index.js and packaging tests; worker/site.js adds production page routing in a separate build stage. Sites review is owner-only and noindex; public launch needs approval, direct-send form/inbox configuration, legal/privacy review, public domain decision and final browser/device QA.

Visual library: /visual-library.html contains 80 reusable SVGs (32 Phosphor icons in two weights plus 16 supporting assets). Use verified Poland geography, no fictional project markers; Warsaw is a city-level contact location only. Networks are conceptual; process labels are proposed and must be checked before publication. No invented impact charts. Photography must show real Polish cities and working environments, with rights and provenance checks; no excessive futuristic AI cityscapes. See public/visual-library/README.md and photography-brief.md. Current scope remains reusable assets and documentation, not full website implementation.

Hover update: use directional colour wipes (right-to-left default for buttons, bottom-to-top for the partnership CTA; left-to-right and top-to-bottom are demonstrated alternatives). A clipped decorative duplicate label keeps contrast during the sweep; it must be aria-hidden. Use 420ms entry, 240ms retreat. Navbar category icons reveal on hover and keyboard focus in reserved slots, without moving neighbouring links. Touch menus show the icons persistently. Reduced motion changes states instantly. This supersedes the earlier plain colour-fade/text-only hover recommendation.

The user likes the deep-plum/mauve background in the innovation hero and wants this used selectively elsewhere. Brand system v2 lives at /brand-kit.html with canonical exports in public/brand-kit. Preserve the supplied Regina logo. Use Cormorant Garamond + DM Sans, Phosphor regular UI icons and light category icons. The earlier blueprint's Newsreader/Inter pairing, blanket 3D prohibition and proposed English pages are superseded by the current selected direction and Hubert's Polish-only brief. Keep the six requested pages: Home, Misja, Działania, Projekty, Partnerstwo, Kontakt. Current scope is kit documentation and component specimens, not implementation of those pages. Do not silently restyle the hero study when revising the kit.

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.
